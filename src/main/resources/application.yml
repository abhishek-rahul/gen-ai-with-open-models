server:
  port: 8080

spring:
  application:
    name: open-models-demo

# Ollama configuration
ollama:
  base-url: http://localhost:11434
  chat-model: llama3.1:8b
  embedding-model: nomic-embed-text

# RAG configuration
rag:
  docs-dir: docs
  chunk-size: 500
  chunk-overlap: 50
  max-results: 5
  min-score: 0.7
